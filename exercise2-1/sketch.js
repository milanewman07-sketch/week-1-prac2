function setup() {
    createCanvas(400, 400);
}

function draw() {
    noStroke();
    background(255)
    fill(0)
    square(0, 0, 200);
    fill(150);
    square(200, 200, 200);
}

// the test thing for this doesnt work but im assuming this is fine

